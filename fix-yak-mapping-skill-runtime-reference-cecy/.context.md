Update skills/yak-mapping/SKILL.md to use --runtime sandbox instead of
--runtime native for interactive Claude Code sessions.

The reviewer noted line 239 still says "Use --runtime native for interactive
Claude Code sessions." This should align with the new default: --runtime sandbox
for implementation work, --runtime native only for research needing unrestricted
network.

Quick grep-and-fix. Check for any other --runtime native references in skills/
that should be updated.

Definition of Done:
- skills/yak-mapping/SKILL.md updated
- Any other stale --runtime native references in skills/ fixed
- No code changes needed
