Re-investigate --permission-mode auto for yak-box workers.

Background: In March 2026 we implemented the switch from --dangerously-skip-permissions
to --permission-mode auto (see done yak: switch-to-auto-permission-mode). The work
was correct but had to be reverted because auto mode requires Team/Enterprise plan
and the operator is on Claude Max (personal).

Check in May 2026:
- Is auto mode now available on Claude Max / personal plans?
- Has the plan requirement changed?
- Are there new permission modes worth evaluating?
- If available, re-apply the changes from switch-to-auto-permission-mode
  (native.go, spawn_sandbox.go use --permission-mode auto; devcontainer keeps
  --dangerously-skip-permissions since Docker is the safety boundary)

Reference commit: ab2bb01 (the original implementation, later reverted)
