Deduplicate OAuth credential detection logic.

The same hasOAuthCredentials/hasOAuthCreds logic exists in:
- internal/runtime/auth.go
- internal/preflight/preflight.go

These implement identical directory-scanning logic under different function names.

Approach:
1. Create a shared implementation (probably in internal/runtime/auth.go or a new internal/auth/ package)
2. Have both call sites use the shared function
3. Remove the duplicate

Also: OAuth credential directory scan duplicated between same two packages.

Verify: go build && go test ./... && shellspec
