Extract rendering functions to src/yak-map/src/render.rs.

## Pattern Being Learned
**Display as adapter** — yx has a DisplayPort trait (src/yaks/src/domain/ports/user_display.rs) with multiple implementations (TuiDisplay, ConsoleDisplay, JsonDisplay). Rendering is an adapter, not core logic.

**Preparing-a-yak** — Spec this with the preparing-a-yak skill before implementing.

## What to Extract
~150 lines of rendering functions:
- task_color()
- status_symbol()
- tree_prefix()
- highlight_line()
- render_task()
- review_status_emoji()

## Prerequisite
Do after "extract color constants" (QW-1) and "agent status kind enum" (QW-5) — those clean up the code that moves here.

## Reference
- yx display port: src/yaks/src/domain/ports/user_display.rs
- yx TUI display: src/yaks/src/adapters/tui_display/mod.rs

## Definition of Done
- render.rs exists with all rendering functions
- State calls render functions, doesnt own them
- Rendering has its own tests
- All existing tests pass
