Extract tree-building logic from the 90-line refresh_tasks() into a TreeBuilder module.

## Pattern Being Learned
**Domain model encapsulates tree operations** — yx YakMap aggregate (src/yaks/src/domain/yak_map.rs) has find_children_of(), get_ancestor_ids() as separate methods. Tree logic is the domain, not infrastructure.

**Preparing-a-yak workflow** — Use the preparing-a-yak skill (src/yaks/skills/preparing-a-yak/SKILL.md) to spec this refactor before implementing. This is a refactoring-type yak, so it gets an ADR (not example mapping).

## What to Extract
refresh_tasks() (lines 249-338) does 4 things:
1. Collect tasks from repository
2. Compute children maps
3. Compute sibling order (is_last)
4. Compute ancestor continuation tracking (for tree lines)

The nested loop at lines 290-331 is the hardest code to follow.

## Reference
- yx domain model: src/yaks/src/domain/yak_map.rs
- yx tree operations: find_children_of(), flatten_depth_first()

## Definition of Done
- TreeBuilder struct or module in src/yak-map/src/tree.rs
- refresh_tasks() becomes: self.tasks = TreeBuilder::build(&self.repo)
- Tree-building logic has its own unit tests
- All existing tests pass
