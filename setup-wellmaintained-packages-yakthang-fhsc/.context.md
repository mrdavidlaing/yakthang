Set up a new yakthang workspace at ~/Work/wellmaintained-packages-yakthang for orchestrating work on the wellmaintained-packages repos (packages, sbomify — already cloned in repos/).

Design principle: Reuse everything from the main yakthang repo (~/Work/yakthang) via symlinks and globally installed tools. The new workspace should be lightweight — just the project-specific config.

What to SYMLINK (pointing back to ~/Work/yakthang):
- agents/yakob.md
- .claude/skills/* (each skill symlinks to ~/Work/yakthang/skills/<name>)

What to CREATE NEW (project-specific):
- yakstead/launch.sh (copy from yakthang, same pattern)
- yakstead/orchestrator.kdl (copy from yakthang, adjust paths/names)  
- .claude/settings.json (project-specific permissions)
- CLAUDE.md (project-specific instructions)
- Justfile (just launch, no build tasks)
- .yaks/ (created by yx init or first yx add)

What is GLOBALLY INSTALLED (already in ~/.local/bin or ~/.local/share):
- yx, yak-box (in PATH)
- yak-map.wasm (in ~/.local/share/zellij/plugins/)

Definition of Done:
- Directory structure created with correct symlinks
- Can run yakstead/launch.sh and get Zellij session with yak-map + yakob
- yx ls works from the new workspace
- Skills available to Claude in the new workspace
