Bug: copyHostOAuthCredentials in spawn_sandbox.go skips copying .credentials.json
if the destination already exists. But worker homes persist across spawns, so
stale OAuth tokens are never refreshed.

Fix: Remove the "skip if exists" check (lines 202-204 of spawn_sandbox.go).
Always overwrite credential files. The managed files list (setupClaudeSettings
outputs) should still be skipped, but .credentials.json and other OAuth files
should always be copied fresh from the host.

File: src/yak-box/internal/runtime/spawn_sandbox.go, copyHostOAuthCredentials()

Change:
  // Skip if the destination already exists (don't overwrite).
  if _, err := os.Stat(dst); err == nil {
      continue
  }
To:
  (delete these lines)

Definition of Done:
- Credentials always copied fresh from host on every spawn
- go test ./... passes
