Deleted src/yak-box/.claude/ (standalone repo artifact; settings.local.json was MCP config only). The desloppify skill inside it is already available in the system; the 'investigate-desloppify-for-yakthang' TODO task covers proper integration, so it was not moved to top-level. Go module path updated from github.com/wellmaintained/yak-box to github.com/wellmaintained/yakthang/src/yak-box across 27 files. AGENTS.md in yak-box stripped of showboat/rtk/opencode sections; yak-map AGENTS.md was already clean. CLAUDE.md symlinks in both subdirs were already in place. just build-yak-box succeeds; full just build fails due to pre-existing rustup toolchain issue (no default Rust toolchain configured) — unrelated to this change.
