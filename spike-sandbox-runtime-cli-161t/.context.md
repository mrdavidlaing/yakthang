Spike: understand @anthropic-ai/sandbox-runtime CLI interface.

This is a RESEARCH task. Do NOT write production code.

Goal: Document how to invoke sandbox-runtime to wrap an arbitrary command
with filesystem + network isolation, so we can design yak-box's sandbox mode.

Steps:
1. Clone or browse https://github.com/anthropic-experimental/sandbox-runtime
2. Read the README and CLI entry point
3. Document:
   - How to invoke it: npx @anthropic-ai/sandbox-runtime <cmd> or direct binary?
   - Filesystem config: how to set allowWrite, denyWrite, denyRead paths
   - Network config: how to set domain allowlists
   - Platform support: Linux (bubblewrap) and macOS (Seatbelt) differences
   - Exit codes and error handling
   - Whether it supports config files or only CLI flags
4. Write a sample wrapper command that would:
   - Allow writes to /home/user/project (CWD)
   - Deny writes to /home/user/project/.yaks/ (except specific subdirs)
   - Allow network to github.com, api.anthropic.com
5. Note any gotchas or limitations

Deliverable: findings written to comments.md on this yak.

Definition of Done:
- CLI interface documented with examples
- Sample wrapper command for yak-box use case
- Platform differences noted
- Gotchas/limitations listed
