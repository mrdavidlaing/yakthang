Clean up old orchestration scripts after yak-box is complete.

## Scripts to Review/Move/Remove
- spawn-worker.sh (replaced by yak-box spawn)
- shutdown-worker.sh (replaced by yak-box stop)
- check-workers.sh (replaced by yak-box check)
- kill-worker.sh (merged into yak-box stop --force)
- cleanup-workers.sh (utility, may keep or merge)
- close-zellij-tab.sh (utility, may keep)
- yak-map.sh (replaced by yak-map Zellij plugin)

## Options
1. Move to scripts/ directory
2. Delete if fully replaced
3. Keep as aliases/wrappers to yak-box

## When
After yak-box CLI is fully functional and can replace these scripts.