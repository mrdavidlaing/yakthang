Added SpawnSandboxDeps() in internal/preflight/preflight.go alongside existing SpawnDevcontainerDeps(). Four new dep vars: Srt (required), Bwrap (required, Linux), Socat (required, Linux), SandboxExec (required, macOS). Uses runtime.GOOS for platform dispatch. Wired into cmd/spawn.go: added "sandbox" to valid --runtime values and routed it through the new preflight function. Test added in preflight_test.go covering the new function with platform-aware assertions.
