Add a preflight check for the sandbox runtime.

Steps:
1. In internal/preflight/preflight.go, add SpawnSandboxDeps() function
2. Check that `srt` binary is in PATH (exec.LookPath("srt"))
3. On Linux: also check for bubblewrap (bwrap) and socat
4. On macOS: check for sandbox-exec (should always be present)
5. Wire into the spawn preflight dispatch for --runtime=sandbox

Error message should tell the user how to install:
  "srt not found. Run: just install-srt (or: npm install -g @anthropic-ai/sandbox-runtime)"

Relevant files:
- src/yak-box/internal/preflight/preflight.go
- src/yak-box/internal/preflight/preflight_test.go

Definition of Done:
- yak-box spawn --runtime sandbox errors clearly if srt is not installed
- Linux checks for bwrap and socat
- macOS checks for sandbox-exec
- go test ./... passes
