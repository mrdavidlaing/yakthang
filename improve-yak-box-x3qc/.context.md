Improve yak-box (Go CLI tool) code quality.

CWD: src/yak-box (in a git worktree off main)
Language: Go

Quality tools available:
- desloppify scan/status/review — code quality scoring and issue tracking
- shellspec — test suite (run: cd src/yak-box && shellspec)
- go test ./... — unit tests
- go build — verify compilation

Current state (from desloppify scan 2026-03-09):
- Objective score: 96.3
- Overall (with subjective): 70.6
- 144 open issues: 51 tier-1, 51 tier-2, 9 tier-3, 33 tier-4
- State file: .desloppify/state-go.json

Top tier-1 themes:
- spawn.go god-file (730 LOC) — mixes flag wiring, orchestration, domain logic
- runSpawn() monolith (264 lines, 12+ concerns)
- Config UnmarshalJSON triple duplication (30+ fields copied 3 ways)
- AI-generated debt: restating comments, boilerplate docstrings
- helpers.go generic naming
- auth.go duplicating runtime logic

Approach for nightshift:
1. Read .desloppify/state-go.json to understand issues
2. Pick a coherent cluster of related tier-1 fixes (e.g. the spawn.go refactor cluster)
3. Implement fixes incrementally — each change should compile and pass tests
4. Run go build && go test ./... && shellspec after each change
5. Create a PR with the improvements

Definition of done:
- go build succeeds
- go test ./... passes
- shellspec passes
- Selected tier-1 issues addressed
- PR created on a feature branch for human review

Scope constraint: Do NOT try to fix all 144 issues. Pick 3-5 related
tier-1 issues that form a coherent improvement. Quality over quantity.
