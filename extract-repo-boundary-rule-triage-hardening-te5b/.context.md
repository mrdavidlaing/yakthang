Extract rule 12 ('Never cross repo boundaries') and pre-spawn checklist item #4 (which references rule 12) from PR #6 into a new, separate PR for further discussion.

What to extract from agents/yakob.md in the PR:
- Rule 12: lines adding 'Never cross repo boundaries. Always --cwd . from release-workspace...'
- Pre-spawn checklist item 4: '--cwd is . or the workspace root: NEVER repos/X — see rule 12'

Steps:
1. Create a new branch from zgagnon:triage-hardening
2. Remove rule 12 and checklist item #4 from agents/yakob.md
3. Push as a separate PR titled something like 'discuss: repo boundary rule for yakob'
4. Force-push the triage-hardening branch without those lines

OR simpler approach:
1. Ask zgagnon to remove rule 12 + checklist item #4 from PR #6
2. Create a new PR with just those additions for discussion

Definition of Done:
- PR #6 no longer contains rule 12 or checklist item #4
- A separate PR exists with just the repo boundary rule for discussion