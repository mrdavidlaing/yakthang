## What
Change the shell pane in worker layouts from 33% height to a fixed 5 lines.

## Where
- src/yak-box/internal/zellij/layout.go
  - Sandboxed layout (~line 29-32): change shell pane from size="33%%" to size=5
  - Native layout (~line 50): change shell pane from size="33%%" to size=5
  - Main build pane: remove the size="67%%" so it fills remaining space (no size attribute = flex fill)
- Update tests in src/yak-box/internal/zellij/layout_test.go accordingly

## Acceptance criteria
- Shell pane is exactly 5 lines high in both sandboxed and native layouts
- Main build pane fills the remaining space
- All tests pass (go test ./...)
- Both layout types (sandboxed + native) are updated consistently
