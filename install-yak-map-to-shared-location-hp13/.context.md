Update the build/install pipeline so yak-map.wasm gets installed to ~/.local/share/zellij/plugins/ instead of staying in the repo's bin/ directory.

## Changes needed

1. **Justfile** — Update the `install-yak-map` recipe:
   - `mkdir -p ~/.local/share/zellij/plugins`
   - `cp src/yak-map/target/wasm32-wasip1/release/yak_map.wasm ~/.local/share/zellij/plugins/yak-map.wasm`
   - Add `install-yak-map` to the `install` recipe's dependencies (currently only has install-yx and install-yak-box)

2. **yakstead/orchestrator.kdl** — Update the plugin location from:
   ```kdl
   plugin location="file:bin/yak-map.wasm"
   ```
   to:
   ```kdl
   plugin location="file:~/.local/share/zellij/plugins/yak-map.wasm"
   ```

3. **bin/README.md** — Remove the yak-map.wasm reference since it no longer lives here. Or update to note it's installed to the shared location now.

4. **bin/yak-map.wasm** — Remove this file (it's now installed to the shared location). Make sure bin/.gitignore or the repo .gitignore still ignores it.

## Acceptance criteria
- `just install` builds and installs all three tools (yx, yak-box, yak-map)
- yak-map.wasm lands at ~/.local/share/zellij/plugins/yak-map.wasm
- orchestrator.kdl references the new location
- No stale yak-map.wasm left in bin/