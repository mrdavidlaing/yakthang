Remove --runtime auto and make --runtime a required explicit flag.

The orchestrator (Yakob) decides the isolation level, not yak-box. Auto-detection
hides a decision that should be visible.

Scope:
- cmd/spawn.go: remove "auto" from flag validation; remove default value;
  make --runtime required (error if not provided)
- Delete DetectRuntime() function entirely
- Remove preflight checks for auto-detection dependencies
- Update --help text to list valid values: native, sandbox, devcontainer

Definition of Done:
- `yak-box spawn` without --runtime errors with clear message listing options
- `--runtime native`, `--runtime sandbox`, `--runtime devcontainer` all work
- DetectRuntime() is deleted
- go test ./... passes

Can run in parallel with "rename sandboxed to devcontainer".
