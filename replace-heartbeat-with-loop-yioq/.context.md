## Goal

Replace the custom `yakob-heartbeat.sh` (fswatch + sleep race) in the yak-shaving-handbook skill with documentation of the `/loop` + `CronCreate` approach that Yakob uses natively.

## What to change

### 1. Delete `skills/yak-shaving-handbook/yakob-heartbeat.sh`
The script is no longer needed.

### 2. Update `skills/yak-shaving-handbook/SKILL.md`

**Section 3 (Heartbeat)** — Replace entirely. The new section should explain:
- Yakob (the orchestrator) uses `/loop` to schedule recurring status checks
- Example: `/loop 5m yx ls; yx field --show <id> agent-status`
- This uses CronCreate under the hood — session-only, auto-expires after 3 days
- No external scripts, no fswatch dependency, no manual relaunching
- Shavers don't need to do anything for heartbeat — it's Yakob's responsibility

**Quick Reference table** — Update the "Launch heartbeat" row to show the /loop command instead of the script path.

**Description frontmatter** — Remove "heartbeat (yakob-heartbeat.sh)" and replace with "heartbeat (via /loop)"

### 3. Keep shaver sections unchanged
Sections 1 (Task lifecycle) and 2 (Message checking) are fine as-is. Don't touch them.

## Acceptance criteria
- yakob-heartbeat.sh is deleted
- SKILL.md section 3 documents the /loop approach
- Quick reference table is updated
- No references to yakob-heartbeat.sh remain in SKILL.md
- Frontmatter description is updated
