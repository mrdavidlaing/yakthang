Refactor agent-status rendering in yak-map AND implement the wip-state rendering layer.

## Scope (expanded during session with David Laing)

This yak covers two things that were worked together:

### 1. Original refactor (structural)
Unify the two-layer priority system (agent-status overrides TaskState) into a single resolution point. Replace duplicated logic in task_color() and status_symbol() with a single ResolvedVisualState::resolve() function.

### 2. wip-state rendering (new feature, agreed in-session)
Implement yak-map rendering support for the new wip-state field (part of the adopt-wip-state-system-numk track). yak-map should:
- Parse wip-state field with 7 values: shaving, blocked, sleeping, ready-for-sniff-test, under-review, failed-sniff-test, ready-for-human
- Render each with a distinct emoji prefix in task lines
- wip-state takes priority over agent-status in ResolvedVisualState resolution
- Backward compat: fall back to legacy agent-status when no wip-state is set

Out-of-scope changes to yakob.md, prompt.go, and skills were handled under adopt-wip-state-system-numk sub-yaks.

## Definition of Done
- ResolvedVisualState::resolve() is the single priority resolution point
- 7 wip-state values rendered with correct emoji
- Backward compat with legacy agent-status
- All existing tests pass (migrated to new API)
- New tests cover wip-state rendering
