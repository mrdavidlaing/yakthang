Extended the original refactor into a full two-layer state system. Layer 1: yx state (todo/wip/done) controlled by humans. Layer 2: wip-state field controlled by Yakob with 7 states (shaving, blocked, sleeping, ready-for-sniff-test, under-review, failed-sniff-test, ready-for-human). Shavers communicate via free-text shaver-message field (no prefix convention). Updated 5 tracked files (model.rs, render.rs, repository.rs, yakob.md, prompt.go) and 3 skill files (handbook, sniff-test, wrap) in .yak-boxes. yak-map has backward compat: falls back to legacy agent-status when no wip-state is set.
