Added ResolvedVisualState enum to model.rs with five variants: Blocked, AgentDone, Wip, TaskDone, Todo. The resolve() constructor centralizes the two-layer priority logic (agent-status overrides TaskState). task_color() and status_symbol() in render.rs now just match on the resolved state — no more duplicated priority checks. AgentStatusKind remains as an internal parsing detail used only by resolve(). All 51 tests pass, no visual behavior changes.
