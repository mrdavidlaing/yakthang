[package]
name = "atty"
version = "0.2.14"
edition = "2021"
description = "Safe replacement for the unmaintained atty crate (RUSTSEC-2021-0145). Uses std::io::IsTerminal instead of the unsafe unaligned read."
license = "MIT"
