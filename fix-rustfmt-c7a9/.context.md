Run cargo fmt to fix formatting inconsistencies in test assertions.
Files affected: src/render.rs, src/util.rs
The issues are in assert! macro formatting — some multi-line, some single-line.

Steps:
1. Run: cargo fmt
2. Verify: bin/dev check passes
3. Commit the formatting fix

This is mechanical — no logic changes.
