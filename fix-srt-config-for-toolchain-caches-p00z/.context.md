Bug: sandbox workers can't write to toolchain caches (Go module cache, Cargo,
npm, Bun, etc.) because GenerateSrtConfig only allows writes to cwd and /tmp.

Failing with: "mkdir /home/user/go/pkg/mod/cache/...: read-only file system"

Fix GenerateSrtConfig in src/yak-box/internal/runtime/srt_config.go to add
common toolchain cache directories to allowWrite:

- GOPATH or ~/go (Go module cache)
- ~/.cargo (Rust)
- ~/.cache (general cache — npm, bun, pip, etc.)
- ~/.local/share/mise (mise tool versions)
- ~/.rustup (Rust toolchains)
- ~/.bun (Bun cache)

Use environment variables where available (GOPATH, CARGO_HOME, etc.) with
fallbacks to default paths. Expand ~ as with existing paths.

Update tests in srt_config_test.go.

Verify: cd src/yak-box && go build && go test ./...

Definition of Done:
- Go module cache writable in sandbox
- Common toolchain caches writable
- Tests updated
- Manual test: sandbox worker can run go build without read-only errors
