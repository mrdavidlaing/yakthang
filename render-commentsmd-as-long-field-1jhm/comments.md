Fixed: show_yak.rs line 93 now checks name.ends_with('.md') — short single-line .md fields (e.g. comments.md with one line) now render as ruled sections below context, not in the header box. Short non-.md fields continue using the newline heuristic. context.md unchanged (already special-cased). Added md_fields_always_render_as_long_sections unit test. All 565 cargo tests pass. Committed as c08f1eb.
