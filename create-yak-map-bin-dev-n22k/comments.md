Created bin/dev with 4 subcommands (check/lint/test/cx). Fixed 8 pre-existing clippy warnings and applied rustfmt to make gate pass on current code. WASM build (wasm32-wasip1) exercised in dev check. Complexity thresholds set at post-fix baseline: cognitive≤29 (max was 28), cyclomatic≤26 (max was 25). rust-code-analysis-cli is not on PATH in the native env — dev check gracefully skips complexity with a warning (same pattern as cargo-audit in the reference). Run via nix-shell for full check with complexity.
