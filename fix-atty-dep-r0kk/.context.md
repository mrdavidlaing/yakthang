Dependabot alert #1: 'atty potential unaligned read' (low severity). The atty crate is in src/yak-map/Cargo.lock. atty is unmaintained — the fix is usually switching to is_terminal or std::io::IsTerminal (Rust 1.70+). For a WASM plugin, TTY detection may be irrelevant — it's likely a transitive dep. Investigate: 1) Which direct dependency pulls in atty? (run cargo tree -i atty in src/yak-map) 2) Can we update/replace that dependency to drop atty? 3) If it's a transitive dep we can't control, document why. Acceptance: Dependabot alert resolved or documented with a clear rationale for why it can't be fixed yet.