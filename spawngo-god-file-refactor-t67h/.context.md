cmd/spawn.go is 730 LOC — a god file mixing flag wiring, orchestration,
and domain logic. runSpawn() alone is 264 lines with 12+ concerns.

This is the HIGHEST IMPACT but also HIGHEST RISK refactor in yak-box.

Approach (if tackled):
1. Extract domain logic (task resolution, skill copying, session orchestration) to internal/
2. Keep cmd/spawn.go as thin CLI wiring
3. Extract runSpawn into smaller coordinator functions

NOT RECOMMENDED for unattended nightshift — too risky without human review
during implementation. Save for a supervised session.

Verify: go build && go test ./... && shellspec
