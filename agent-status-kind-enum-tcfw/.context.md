Create an AgentStatusKind enum to replace duplicated prefix parsing.

## Pattern Being Learned
**Parse once, carry meaning in types** — yx uses YakState enum with FromStr impl (src/yaks/src/domain/yak_state.rs lines 25-39). Parsing happens at the boundary; the rest of the code works with the enum.

## What to Change
task_color() (lines 341-350) and status_symbol() (lines 359-366) both parse agent-status string prefixes ("blocked:", "done:", "wip:"). Create an enum, parse once, consume in both places.

## Reference
Read src/yaks/src/domain/yak_state.rs for the pattern — FromStr impl, Display impl, exhaustive match.

## Definition of Done
- AgentStatusKind enum with variants (Blocked, Done, Wip, Unknown)
- FromStr or from_status_string() constructor
- task_color() and status_symbol() consume the enum
- All tests pass
