Split main.rs into a proper multi-file module structure.

## Pattern Being Learned
**Module architecture** — yx has domain/, application/, adapters/, infrastructure/. yak-map is simpler, but the principle of clear module boundaries applies.

## Prerequisite
This yak should be trivial IF the preceding work is done:
- util.rs (from QW-2)
- render.rs (from MR-1)
- tree.rs (from MR-2)
- Individual handler methods (from MR-3)
- TaskSource trait (from MR-4)

## Target Structure
src/yak-map/src/
├── main.rs        (~100 lines: ZellijPlugin impl, load/update/render dispatch)
├── lib.rs         (module declarations, re-exports)
├── model.rs       (TaskState, TaskLine, AgentStatusKind — domain types)
├── repository.rs  (TaskRepository — filesystem adapter, TaskSource trait)
├── tree.rs        (TreeBuilder — already extracted in MR-2)
├── render.rs      (already extracted in MR-1)
├── clipboard.rs   (OSC 52, shell escaping)
└── util.rs        (already extracted in QW-2)

## Definition of Done
- main.rs is <150 lines
- Each module has clear responsibility
- No circular dependencies between modules
- All tests pass
- bin/dev check passes
