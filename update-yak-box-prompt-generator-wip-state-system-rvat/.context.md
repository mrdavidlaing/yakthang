Update the yak-box prompt generator to use shaver-message instead of agent-status.

The prompt.go template injects agent-status reporting instructions into every
shaver's prompt.txt. These need to change:
- Replace agent-status field references with shaver-message
- Remove the wip:/blocked:/done: prefix convention
- Tell shavers to write free-text messages describing what they're doing
- Remove any instructions about shavers controlling state

Source: src/yak-box/internal/prompt/prompt.go

Definition of Done:
- Generated prompt.txt uses shaver-message instead of agent-status
- No prefix convention in shaver instructions
- Shaver told explicitly they don't control wip-state
- go test ./... passes in src/yak-box/
