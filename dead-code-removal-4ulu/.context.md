Remove dead/unreachable code.

Specific items:
1. resolveAnthropicKeySuffix — documented contract but nothing calls it. Delete it.
2. messageCmd — defined and tested but never registered with rootCmd. Either register or delete.
3. generateDevContainerID — unreachable "return encoded" branch; always-true len guard makes else path dead.
4. generateNativeWrapperScript — returns (content, paneName) but all callers discard paneName. Simplify return.

Approach: Delete dead code. For messageCmd, check if it was meant to be registered
(look at git log). If unclear, delete it.

Verify: go build && go test ./... && shellspec
