Revert --permission-mode auto back to --dangerously-skip-permissions.

Auto mode requires Team/Enterprise plan. Operator is on Claude Max (personal)
which doesn't support it.

Files to revert:
- src/yak-box/internal/runtime/native.go: change --permission-mode auto back to --dangerously-skip-permissions
- src/yak-box/internal/runtime/spawn_sandbox.go: same
- src/yak-box/internal/runtime/helpers_test.go: update test assertion back
- agents/yakob.md: revert any auto mode references in spawn guidance
- yakstead/orchestrator.kdl: already reverted manually

Keep the switch-to-auto-permission-mode yak as done — it was correct work,
just not usable on the current plan. When upgrading to Team plan, re-apply.

Definition of Done:
- All workers use --dangerously-skip-permissions
- go test ./... passes
- No references to --permission-mode auto in production code
