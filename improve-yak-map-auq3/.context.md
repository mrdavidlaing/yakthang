Improve yak-map (Rust/WASM Zellij plugin) code quality.

CWD: src/yak-map (in a git worktree off main)
Language: Rust, compiled to wasm32-wasip1

Quality tools available:
- bin/dev check — runs: WASM build, clippy, rustfmt, tests, complexity analysis
- bin/dev cx — complexity only (needs rust-code-analysis-cli)
- desloppify scan/review — broader code quality scoring

Current state (from desloppify scan 2026-03-08):
- Objective score: 89.2 (good)
- 1 open structural issue: src/main.rs is 1293 LOC (too large)
- Complexity baseline: build() cognitive=22, cyclomatic=21 (highest)
- 20 subjective dimensions unscored

Approach:
1. Run bin/dev check to get current baseline
2. Split main.rs into logical modules (the big structural win)
3. After splitting, re-run bin/dev check — all must pass
4. Look for complexity reduction opportunities in the split modules
5. Tighten complexity thresholds in bin/dev if possible

Definition of done:
- bin/dev check passes (WASM build + lint + test + complexity)
- main.rs significantly smaller (target: no file >400 LOC)
- No regressions: all existing tests pass
- PR created on a feature branch for human review

Key constraint: This is a WASM plugin — must build for wasm32-wasip1.
Some crate features may not be available in WASM.
