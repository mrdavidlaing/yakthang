Improve yak-map (Rust/WASM Zellij plugin) code quality.

CWD: src/yak-map (in a git worktree off main)
Language: Rust, compiled to wasm32-wasip1

Quality tools:
- bin/dev check — runs: WASM build, clippy, rustfmt, tests, complexity analysis
- bin/dev cx — complexity only (needs rust-code-analysis-cli via nix)

Current file structure (already refactored from 1293-line main.rs):
  render.rs     643 LOC  (largest — rendering logic)
  repository.rs 350 LOC  (task data loading)
  main.rs       283 LOC  (plugin entry point)
  model.rs      229 LOC  (data models)
  util.rs       213 LOC  (utilities)
  tree.rs       168 LOC  (tree building)

Current bin/dev check status:
- ✅ WASM build passes
- ✅ Clippy passes (no warnings)
- ❌ rustfmt fails — formatting inconsistencies in test assertions in render.rs and util.rs
- Tests and complexity not yet reached (lint failure stops the run)

Approach:
1. Fix the rustfmt issues first (cargo fmt)
2. Run bin/dev check to get a full passing baseline
3. Run bin/dev cx for complexity analysis
4. Look for further improvements: render.rs at 643 LOC may benefit from splitting
5. Check for any clippy improvements with stricter lints
6. Consider adding more tests if coverage is thin

Definition of done:
- bin/dev check passes completely (WASM + lint + tests + complexity)
- Code quality measurably improved (formatting fixed, complexity reduced, or better structure)
- No regressions
- PR created on a feature branch for human review

Key constraint: This is a WASM plugin — must build for wasm32-wasip1.
