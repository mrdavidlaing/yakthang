## Goal

The just install recipe should build and install all three tools, including yak-map.

Current:
  install: install-yx install-yak-box

Should be:
  install: install-yx install-yak-box install-yak-map

Also update install-yak-map to actually copy the wasm file to the zellij plugin dir instead of just printing a message. Check where zellij plugins live on this machine (likely ~/.config/zellij/plugins/ or ~/.local/share/zellij/plugins/).
