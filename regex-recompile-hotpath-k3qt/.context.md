substituteString recompiles the same regexp on every invocation,
inside a ReplaceAllStringFunc callback (hot path during config substitution).

File: likely in pkg/devcontainer/ or internal/ — search for substituteString.

Fix: Compile the regexp once as a package-level var or in an init block,
then reference it in the function.

Verify: go build && go test ./...
