pass: design doc covers all deliverables (auth detection, auth-status cmd, per-shaver OAuth flow, API key unchanged, open questions addressed); all claims about existing code verified against codebase
