Bug observed during session-name-flag worker shutdown:

`./bin/yak-box stop --name session-name-flag` output:
```
Stopping worker: session-name-flag
Clearing task assignments...
Cleared assignment: yak-box/session-name-flag
Closing Zellij tab...
Worker stopped: session-name-flag
```

But the tab "Yakov 🦬🔔 yak-box/session-name-flag" remained open.

The stop command reported success closing the tab, but didn't actually do it.
Had to manually close with:
```
zellij action go-to-tab-name "Yakov 🦬🔔 yak-box/session-name-flag"
zellij action close-tab
```

This affects native workers. Need to investigate:
1. How yak-box stop identifies the tab to close
2. Whether the zellij action is failing silently
3. Tab name matching (emoji + task path might be causing issues)