Updated agents/yakob.md in three places:
1. Key flags section (line ~194): changed --runtime description to default sandbox, native only for research/git-push
2. Example spawn (line ~281): changed --runtime native to --runtime sandbox
3. Added Runtime Selection table (line ~415) with task-type guidance and added runtime check to Pre-Spawn Checklist

Left yak-nightshift/SKILL.md unchanged — it already has its own guidance noting native is needed for git push overnight.
