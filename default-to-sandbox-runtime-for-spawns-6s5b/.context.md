Update Yakob's spawn behavior to default to --runtime sandbox for most workers.

Now that sandbox runtime is fully operational (srt + pre-auth + auto mode),
Yakob should prefer sandbox isolation over native for implementation shavers.

Rules:
- **Implementation shavers** (writing code, refactoring, tests): --runtime sandbox
  - Filesystem isolation protects against accidental cwd escapes
  - Network allowlist prevents unintended external calls
  - Auto mode provides intent verification

- **Research shavers** (web fetch, browsing repos, API exploration): --runtime native
  - Network allowlist is too restrictive for arbitrary web research
  - These are read-only tasks with low blast radius

- **Review subagents**: No change — these use the Agent tool, not yak-box spawn

Update location: This is a behavior change in Yakob's orchestrator prompt
(skills/yakob or AGENTS.md), not a yak-box code change. Yakob decides the
runtime per-spawn based on the task type.

Suggested prompt addition for Yakob:
  "When spawning shavers, use --runtime sandbox by default. Use --runtime native
  only for research tasks that need unrestricted network access (web fetches,
  browsing external repos, API exploration)."

Definition of Done:
- Yakob's instructions updated to default to --runtime sandbox
- Clear guidance on when to use native vs sandbox
- Next session's spawns use sandbox by default
