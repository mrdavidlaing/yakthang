Removed setupClaudeSettings call from SpawnNativeWorker and stripped CLAUDE_CONFIG_DIR from generateNativeWrapperScript. Native workers now use the operator's real ~/.claude/ directly. Replaced _ANTHROPIC_API_KEY + apiKeyHelper pattern with ANTHROPIC_API_KEY set directly in the wrapper env. Kept setupClaudeSettings function itself since sandbox and devcontainer runtimes still use it. Updated all test assertions accordingly.
