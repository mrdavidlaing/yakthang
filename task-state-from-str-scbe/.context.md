Add FromStr impl to TaskState enum to replace inline magic string matching.

## Problem
TaskState parsing in get_task() (main.rs ~lines 136-139) uses inline string literals:
  Some("wip") => TaskState::Wip,
  Some("done") => TaskState::Done,
  _ => TaskState::Todo,

This should use a proper FromStr impl, matching the pattern in yx's YakState (src/yaks/src/domain/yak_state.rs lines 25-39).

## What to Do
1. Add a FromStr impl (or from_str method) to the existing TaskState enum
2. Replace the inline matching in get_task() with TaskState::from_str() or .parse()
3. Check for any other places TaskState is parsed from strings and convert those too

## Reference
- TaskState enum already exists in main.rs (around line 22)
- YakState in src/yaks/src/domain/yak_state.rs for the FromStr pattern
- AgentStatusKind.from_status_string() in main.rs for the local pattern

## Definition of Done
- TaskState has FromStr impl or from_str constructor
- No inline "wip"/"done" string matching remains outside the constructor
- All tests pass
- bin/dev check passes