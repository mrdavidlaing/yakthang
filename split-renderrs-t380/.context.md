render.rs is 643 LOC — the largest file in the project.
Consider splitting into:
- render.rs — main render_frame logic
- render_task.rs — individual task line rendering + task color helpers
- render_detail.rs — detail pane rendering (if substantial)

Constraints:
- Must build for wasm32-wasip1
- All 70 existing tests must pass
- bin/dev check must pass after split

Approach: Look at the logical groupings in render.rs, identify clean
module boundaries, extract with pub(crate) visibility.
