## What
Change the done icon from ● (filled circle) to ✓ (checkmark) in yak-map's task rendering.

## Where
- src/yak-map/src/main.rs
  - `status_symbol()` function (~line 359-372): change the return value for done states from '●' to '✓'
  - Update any tests that assert on the done icon character

## Acceptance criteria
- Done yaks show ✓ instead of ● in the yak-map display
- WIP yaks still show ● (unchanged)
- Todo yaks still show ○ (unchanged)
- All existing tests pass (update assertions as needed)
- Colors remain the same (green for agent-status done, dark gray for state done)
