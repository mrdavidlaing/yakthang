Update the yak-shaving-handbook skill to use the new wip-state system.

Key changes:
- Shavers no longer write agent-status with wip:/blocked:/done: prefixes
- Instead, shavers write free-text progress to `shaver-message` field
  e.g. echo "tests pass, I think this is done" | yx field <id> shaver-message
- Shavers do NOT set wip-state — Yakob owns all state transitions
- Remove all `echo "wip: ..." | yx field <id> agent-status` instructions
- Replace with `echo "..." | yx field <id> shaver-message` instructions
- Keep comments.md for detailed end-of-work narrative (unchanged)
- Keep yakob-message for downward communication (unchanged)

Source: .yak-boxes/@home/Yakueline/.claude/skills/yak-shaving-handbook/SKILL.md

Definition of Done:
- Handbook references shaver-message instead of agent-status for live signals
- No more prefix convention (wip:/blocked:/done:) in shaver instructions
- Shavers told explicitly they do not control wip-state
- All existing sections updated consistently
- Handbook still makes sense as a standalone document
