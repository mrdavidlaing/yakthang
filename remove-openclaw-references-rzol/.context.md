## Goal

Remove all references to OpenClaw/openod from the yakthang repo. Yakob now runs as a Claude Code agent, not an OpenClaw instance.

## Files to update

Found via grep -ri openclaw/openod:
- cost-openclaw.sh — likely delete entirely
- THEMING-VERIFICATION.md — update or delete
- setup-vm.sh — remove openclaw setup steps
- docs/cost-tracking.md
- docs/repo-layout.md
- docs/deployment/SLACK-SETUP.md
- docs/deployment/OPERATIONS.md
- docs/deployment/TROUBLESHOOTING.md
- docs/deployment/DEPLOYMENT.md
- docs/deployment/SECURITY.md
- README.md — update to describe Claude Code agent approach
- cost-summary.sh — remove openclaw cost references
- .yaks/ context files — leave these alone, they're historical
- yakstead/orchestrator.kdl — update comments
- src/yak-map/Cargo.toml — update author email from yakob@openod
- src/yak-map/plugin.yaml — update author email from yakob@openod

## Approach

- Delete files that are purely openclaw-related (cost-openclaw.sh, deployment docs)
- Update files that mention openclaw but have broader purpose (README, setup-vm.sh, orchestrator.kdl)
- Replace openod author references with appropriate alternatives
- Leave .yaks/ context files untouched — they're historical records

## Acceptance criteria
- grep -ri openclaw returns only .yaks/ context files
- grep -ri openod returns nothing (or only .yaks/)
- README accurately describes the current Claude Code agent architecture
- Clean commit


## Additional: orchestrator.kdl specifics

In yakstead/orchestrator.kdl:
- Line comment "Right 2/3: OpenClaw TUI + shell" -> "Right 2/3: Claude Code + shell"
- Line comment "Each worker gets its own tab with an opencode pane" -> "Each worker gets its own tab via yak-box spawn"
- Line comment "spawn-worker.sh" -> "yak-box spawn"
- The command "claude" is correct (already updated)
- The cwd "/home/yakob/yakthang" is a stale VM path — needs updating or removing
- Top comment "orchestrator opencode instance" -> "orchestrator Claude Code instance"
