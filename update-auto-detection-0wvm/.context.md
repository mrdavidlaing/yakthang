Update --runtime auto detection to include sandbox tier.

Current detection (internal/runtime/sandboxed.go:82-92):
  Docker running? → sandboxed
  Zellij available? → native
  else → unknown

New detection order:
  Docker running? → devcontainer
  sandbox-runtime available? → sandbox
  Zellij available? → native
  else → unknown

Source: internal/runtime/sandboxed.go DetectRuntime() (will be in
devcontainer.go after rename)

Also update the --runtime flag help text and validation to accept:
auto, devcontainer, sandbox, native (+ deprecated alias "sandboxed")

Definition of Done:
- DetectRuntime() checks devcontainer → sandbox → native
- Flag accepts all four values + deprecated alias
- Tests verify detection order
- go test ./... passes

Depends on: "rename sandboxed to devcontainer" + "add sandbox runtime mode"
