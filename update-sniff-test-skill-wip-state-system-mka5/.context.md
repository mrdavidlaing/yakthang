Update yak-sniff-test to use wip-state instead of review-status/review-verdict.

Key changes:
- Yakob sets wip-state transitions, not the reviewer
- Before spawning reviewer: echo "under-review" | yx field <id> wip-state
  (replaces: echo "in-progress" | yx field <id> review-status)
- On pass: echo "ready-for-human" | yx field <id> wip-state
  (replaces: echo "pass: ..." | yx field <id> review-status + review-verdict)
- On fail: echo "shaving" | yx field <id> wip-state + message the shaver
  (replaces: echo "fail: ..." | yx field <id> review-status + creating sub-yak)
- review-notes field STAYS for failure evidence detail
- Remove all review-status and review-verdict field writes
- Reviewer agent still produces pass/fail/needs-info verdict text;
  Yakob reads it and drives the wip-state transition

Source: .yak-boxes/@home/Yakueline/.claude/skills/yak-sniff-test/SKILL.md

Definition of Done:
- Skill uses wip-state transitions instead of review-status/review-verdict
- Pass → ready-for-human, fail → shaving + yakob-message to shaver
- review-notes still written on failure
- No references to review-status or review-verdict fields
- Skill reads coherently as standalone
