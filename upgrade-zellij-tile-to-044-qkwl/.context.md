Upgrade yak-map's zellij-tile dependency from 0.43 to 0.44.

The plugin is panicking after upgrading Zellij to 0.44 — likely due to
breaking API changes between zellij-tile 0.43 and 0.44.

Steps:
1. Update Cargo.toml: zellij-tile = "0.44" (check crates.io for availability)
2. Run cargo build --target wasm32-wasip1 and fix any compilation errors
3. Check for breaking changes in:
   - ZellijPlugin trait (load, update, render signatures)
   - Event enum variants (Timer, Key)
   - Permission model (PermissionType enum)
   - API functions (set_timeout, subscribe, open_command_pane_floating, etc.)
4. Fix the unwrap() panic risk at src/yak-map/src/util.rs:126 — replace with
   safe pattern matching
5. Update the zellij-utils dependency if also needed
6. Test: cargo build --target wasm32-wasip1 --release
7. Install: just install-yak-map (or cp to ~/.local/share/zellij/plugins/)
8. Verify plugin loads in Zellij 0.44 without panicking

If zellij-tile 0.44 is not yet published on crates.io, check:
- GitHub releases at https://github.com/zellij-org/zellij
- Whether we can use a git dependency temporarily

Relevant files:
- src/yak-map/Cargo.toml
- src/yak-map/src/main.rs (ZellijPlugin impl)
- src/yak-map/src/util.rs (unwrap fix at line 126)

Definition of Done:
- zellij-tile upgraded to 0.44 (or latest compatible)
- Plugin compiles without warnings
- Plugin loads in Zellij 0.44 without panicking
- Navigation (j/k) and timer refresh work correctly
- unwrap() at util.rs:126 replaced with safe pattern
