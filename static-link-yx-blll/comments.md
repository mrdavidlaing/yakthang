The upstream yx release binary (github.com/mattwynne/yaks/releases/download/latest/) is already statically linked. Instead of modifying Cargo.toml to vendor libgit2/libssh2, replaced the COPY-based yx install in the Dockerfile with the upstream install.sh script. Also removed the yx binary staging step from setup-vm.sh (no longer needed) and added unzip to apt deps (required by install.sh). Verified: yx 0.1.0 runs and ldd confirms 'statically linked' in Ubuntu 24.04 container.
