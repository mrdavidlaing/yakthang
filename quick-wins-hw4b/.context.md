Four small, safe refactors. Each exercises a basic pattern and takes <30 min.

## Work Order (these are independent — any order works)
1. remove allow unused — compiler as quality tool
2. extract color constants — named constants over magic values
3. agent status kind enum — parse once, carry meaning in types
4. extract utilities module — first multi-file step

## Process Pattern
Each yak: read context → implement → run bin/dev check → commit.
These are warm-up exercises before the deeper structural refactors.
