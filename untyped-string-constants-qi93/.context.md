Runtime, Tool, Mode, and SecurityWarning.Severity are untyped strings
with scattered raw comparisons throughout the codebase.

Examples:
- Runtime: "sandboxed" / "native" / "unknown"
- Tool: string comparisons
- Mode: string comparisons
- SecurityWarning.Severity: "warning" / "critical"

Fix: Define typed constants or string enums for each.
Replace raw string comparisons with typed constant references.

Verify: go build && go test ./... && shellspec
