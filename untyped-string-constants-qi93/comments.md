Defined typed constants for Runtime (native/sandbox/devcontainer), Tool (claude/cursor/opencode), Mode (plan/build), and SecurityWarning.Severity (warning/critical) in pkg/types/types.go and pkg/devcontainer/security.go. Replaced all raw string comparisons in production code across spawn.go, stop.go, native.go, spawn_sandbox.go, spawn_devcontainer.go, layout.go, opencode.go, and preflight.go. Test code still uses string literals in struct initializers (these match the constant values). go build, go test ./... all pass. shellspec sandbox_smoke failures are pre-existing (read-only GOCACHE in sandbox env).
