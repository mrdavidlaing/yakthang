Rename --runtime sandboxed to --runtime devcontainer throughout yak-box.

This is a pure rename/refactor — no behavior changes. The Docker-based runtime
is really a devcontainer runtime; we're freeing up "sandbox" for the lighter
@anthropic-ai/sandbox-runtime approach.

Scope:
- cmd/spawn.go: flag validation (line 88-90), runtime detection, variable names
- internal/runtime/sandboxed.go → rename file to devcontainer.go; rename functions
- internal/runtime/helpers.go: any references to "sandboxed"
- internal/preflight/preflight.go: SpawnSandboxedDeps → SpawnDevcontainerDeps
- pkg/types/types.go: Runtime field values
- Tests: update all "sandboxed" string literals
- Keep "sandboxed" as a deprecated alias that maps to "devcontainer" with a warning

Definition of Done:
- `--runtime devcontainer` works identically to current `--runtime sandboxed`
- `--runtime sandboxed` still works but prints deprecation warning
- `--runtime auto` detects devcontainer (Docker) before sandbox before native
- All tests pass: go test ./...
- No remaining references to "sandboxed" except the deprecation alias
