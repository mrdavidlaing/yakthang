Add "sandbox" as a valid --runtime value in yak-box spawn.

Steps:
1. cmd/spawn.go: add "sandbox" to the runtime flag validation list
2. Route --runtime=sandbox to a new SpawnSandboxWorker function in internal/runtime/
3. Create internal/runtime/spawn_sandbox.go with a stub SpawnSandboxWorker that
   returns an error "sandbox runtime not yet implemented" for now
4. Add to stop.go dispatch as well (StopSandboxWorker stub)

Keep it minimal — just the flag routing. Config generation and srt invocation
come in later yaks.

Relevant files:
- src/yak-box/cmd/spawn.go (flag validation ~line 88-90, runtime dispatch)
- src/yak-box/cmd/stop.go (stop dispatch)
- src/yak-box/internal/runtime/spawn_sandbox.go (new file)

Definition of Done:
- `yak-box spawn --runtime sandbox --cwd . --yak-name test` routes to SpawnSandboxWorker
- `yak-box spawn --runtime invalid` still errors
- go test ./... passes
