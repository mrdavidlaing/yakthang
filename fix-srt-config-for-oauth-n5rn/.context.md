Fix: Claude OAuth login fails inside sandbox because srt blocks local port binding.

The OAuth flow needs to:
1. Listen on localhost for the callback redirect (requires allowLocalBinding: true)
2. Reach anthropic.com for the OAuth authorization (not just api.anthropic.com)

Changes needed in src/yak-box/internal/runtime/srt_config.go:

1. Add AllowLocalBinding field to SrtNetworkConfig:
   AllowLocalBinding bool `json:"allowLocalBinding"`

2. Set it to true in GenerateSrtConfig:
   AllowLocalBinding: true

3. Verify "anthropic.com" and "*.anthropic.com" are both in allowedDomains
   (*.anthropic.com should already cover it, but add "anthropic.com" explicitly
   to be safe — some implementations don't match the bare domain with a wildcard)

4. Also add "console.anthropic.com" explicitly — the OAuth flow redirects through it

5. Update tests in srt_config_test.go

Run: cd src/yak-box && go test ./...

Definition of Done:
- SrtNetworkConfig includes AllowLocalBinding: true
- OAuth-related domains in allowedDomains
- go test ./... passes
- yak-box spawn --runtime sandbox can complete OAuth login
