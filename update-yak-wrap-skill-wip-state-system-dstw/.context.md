Update yak-wrap to read wip-state and shaver-message instead of agent-status.

Key changes in Phase 1 (Harvest):
- Reconcile step: check wip-state instead of agent-status for done detection
  (though with the new model, shavers don't set done — Yakob does, so the
  reconciliation logic changes: look for tasks where wip-state is
  "ready-for-human" and yx state is still wip)
- Read shaver-message instead of agent-status for the outcome summary
- Read wip-state for workflow position
- comments.md stays unchanged

Source: .yak-boxes/@home/Yakueline/.claude/skills/yak-wrap/SKILL.md

Definition of Done:
- Harvest reads shaver-message and wip-state instead of agent-status
- Reconcile step updated for new authority model
- No references to agent-status prefix convention
- Skill reads coherently as standalone
