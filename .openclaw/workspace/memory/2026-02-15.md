# 2026-02-15 Session Notes

## Repo Layout Decision
- Agreed on yakthang repo structure: `docs/`, `src/`, `bin/`, `tmp/`, `.yaks/`, `.openclaw/`
- `src/` contains: `yaks/` (yx CLI, Rust), `yak-map/` (Zellij plugin, Rust/WASM), `yak-box/` (Docker orchestration, Go)
- Kebab-case for directory names (yak-box, yak-map) — operator confirmed this preference
- `bin/` holds compiled output; for non-dev usage you only need `bin/` + `docs/` + `.yaks/`
- Docker containers mount the whole yakthang/ directory; scoping via yx tasks, git as safety net
- Spec written to `docs/repo-layout.md`

## yak-box Scaffold Complete
- Full Go project in `src/yak-box/` with cobra CLI: spawn, stop, check commands
- Internal packages: config, metadata, persona, prompt, runtime (sandboxed + native), zellij
- Tests with testify
- Compiled binary at `bin/yak-box` (stubs only, not functional yet)
- Design doc at `docs/yak-box-design.md` (246 lines)
- Detailed plan at `.sisyphus/plans/yak-box-scaffold.md`
- Worker spawned to implement actual spawn command (Yakueline yak-box-impl)

## yak-map Scaffold Started
- Rust project initialized in `src/yak-map/` with Cargo.toml, zellij-tile dep, basic lib.rs
- Design doc at `docs/yak-map-design.md`
- Build artifacts gitignored (`src/yak-map/target/`)

## Worker Image Switch
- Switched `spawn-worker.sh` from `yak-worker:latest` to `yak-devcontainer:latest`
- devcontainer image includes: Go 1.26, Rust + wasm32-wasip1 target, opencode CLI, yx, git safe.directory config
- Image is ~2.3GB vs old 469MB but has all build tools workers need
- `.devcontainer/Dockerfile` updated with yx binary and git config

## Zellij Session
- Current zellij session is `didactic-horse` (not `yakthang` — shutdown scripts warn about this but it's harmless)

## Active Work
- yak-box-impl worker running on OLD image (yak-worker:latest) implementing spawn command
- yak-map work paused (worker shut down)

## Rust Development Note
- ALWAYS use `--resources heavy` (2 CPUs, 4GB memory, 1024 PIDs) when spawning Rust/WASM workers
- Default/light containers may OOM during Rust compilation (especially WASM targets)
