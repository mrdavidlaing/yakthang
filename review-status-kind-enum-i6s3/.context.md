Create a ReviewStatusKind enum to replace starts_with string matching in review_status_emoji().

## Problem
review_status_emoji() (in main.rs) uses starts_with("pass"), starts_with("fail"), starts_with("in-progress") — the same anti-pattern that agent-status-kind-enum just fixed for AgentStatusKind.

## What to Do
Follow the exact same pattern as AgentStatusKind:
1. Create a ReviewStatusKind enum with variants: Pass, Fail, InProgress, Unknown
2. Add a from_status_string() constructor that parses the prefix once
3. Refactor review_status_emoji() to match on the enum exhaustively
4. Use the enum anywhere else review-status strings are parsed

## Reference
- AgentStatusKind in src/main.rs (the pattern to copy)
- review_status_emoji() in src/main.rs (the code to fix)

## Definition of Done
- ReviewStatusKind enum exists with Pass, Fail, InProgress, Unknown variants
- from_status_string() constructor parses prefixes in one place
- review_status_emoji() uses exhaustive match on the enum
- No starts_with calls for review status remain outside the constructor
- All tests pass
- bin/dev check passes